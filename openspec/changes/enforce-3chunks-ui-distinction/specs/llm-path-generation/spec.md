# llm-path-generation (Delta Spec)

## MODIFIED

- **REQ-VARIABLE-CHUNKS-COUNT**: The LLM prompt SHALL enforce EXACTLY 3 variableChunks per chunk. The prompt MUST include explicit rule: "You MUST generate EXACTLY 3 variableChunks for each chunk" with examples showing 3 chunks.
